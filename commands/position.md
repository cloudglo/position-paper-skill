---
description: Produce a position paper (BUY / WAIT / NO BUY) on a company or product. Supports --focus, --context, --save.
argument-hint: <subject> [--focus <area>] [--context "<desc>"] [--save]
---

Invoke the `position-paper-skill:position-paper-researcher` skill using the Skill tool.

Pass the user's arguments through verbatim: `$ARGUMENTS`

Follow the skill's workflow exactly — parse the subject and flags, run the three parallel research tracks, and synthesize the final position paper. Do not substitute your own workflow for the skill's.
